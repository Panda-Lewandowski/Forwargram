services:
  relaygram:
    build: .
    image: ${IMAGE_NAME:-relaygram:latest}
    env_file:
      - ${ENV_FILE:-.env}
    volumes:
      - ./:/config:ro
    restart: unless-stopped
    stdin_open: true
    tty: true
